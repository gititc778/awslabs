FROM nginx:1.27-alpine

# Static UI
COPY index.html /usr/share/nginx/html/index.html

# nginx serves the app and proxies /api/ to the Flask backend
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 8080
CMD ["nginx", "-g", "daemon off;"]
